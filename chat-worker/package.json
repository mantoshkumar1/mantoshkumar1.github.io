{
  "name": "personal-website-chat-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:unit": "node --test",
    "evaluate": "node eval/run-evaluation.mjs --check",
    "evaluate:update": "node eval/run-evaluation.mjs --write",
    "test": "npm run test:unit && npm run evaluate"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
